import Vue from 'vue';
import Vuetify from 'vuetify/lib/framework';

// Sử dụng Vuetify trong ứng dụng
Vue.use(Vuetify);

// Xuất một instance mới của Vuetify với cấu hình mặc định
export default new Vuetify({
  // Bạn có thể thêm các cấu hình tùy chọn của Vuetify tại đây nếu cần
});
